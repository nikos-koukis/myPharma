{
  "name": "mypharma",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "scrape": "tsx scripts/scrape.ts",
    "sync-regions": "tsx scripts/sync-regions.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:generate": "prisma generate --schema=src/db/prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema=src/db/prisma/schema.prisma",
    "db:push": "prisma db push --schema=src/db/prisma/schema.prisma",
    "help": "echo '\n📋 myPharma Backend Commands\n─────────────────────────────────────────\n  npm run dev            Start dev server (API + crons)\n  npm run scrape         Run pharmacy scraper (all cities)\n  npm run scrape -- --city patra       Scrape only Patra\n  npm run scrape -- --region ΑΧΑΪΑΣ    Scrape only Achaia region\n  npm run scrape -- --help             Show scraper options\n  npm run sync-regions   Refresh region/city list from vrisko.gr\n  npm run build          Compile TypeScript\n  npm run start          Start production server\n  npm run db:generate    Generate Prisma client\n  npm run db:migrate     Run DB migrations\n  npm run db:push        Push schema to DB\n─────────────────────────────────────────\n⚙️  Crons (automatic in dev/start):\n  Daily 6AM    → Pharmacy scrape\n  Sunday 5AM   → Region refresh\n─────────────────────────────────────────\n🚀 First time setup:\n  1. npm run db:push\n  2. npm run sync-regions\n  3. npm run scrape\n'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/swagger": "^9.6.1",
    "@fastify/swagger-ui": "^5.2.5",
    "@prisma/client": "^5.22.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.7.2",
    "ioredis": "^5.9.2",
    "node-cron": "^4.2.1",
    "playwright": "^1.58.0",
    "playwright-extra": "^4.3.6",
    "prisma": "^5.22.0",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
